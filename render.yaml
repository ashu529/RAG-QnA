services:
  - type: web
    name: rag-assistant
    runtime: python
    buildCommand: ./build.sh
    startCommand: cd backend && waitress-serve --port=$PORT app:app
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
